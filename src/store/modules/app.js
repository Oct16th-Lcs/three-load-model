const app = {
    state: {
        loading: false
    },
    mutations: {
        SET_LOADING: (state, loading) => {
            state.loading = loading
        }
    },
    actions: {}
};

export default app;
